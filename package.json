{
  "name": "hormuz",
  "version": "0.1.0",
  "description": "Hormuz — an asymmetric two-player strait-running board game (US fleet vs. Iran hidden mines/ambush). Vanilla JS, no bundler.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=16.17.0"
  },
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8000"
  }
}
